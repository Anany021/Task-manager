# ----- Frontend environment -----
# Copy to `.env` and adjust. Vite only exposes vars that start with VITE_.

# Backend API base URL.
# Local dev:    http://localhost:5000
# Railway prod: https://your-server.up.railway.app
VITE_API_URL=http://localhost:5000
